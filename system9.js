const mt=document.getElementById('menuToggle');if(mt)mt.addEventListener('click',()=>{const a=document.getElementById('app');if(innerWidth<=980)a.classList.toggle('mobile-open');else a.classList.toggle('collapsed')});
function openModal(t,h){document.getElementById('modalTitle').textContent=t;document.getElementById('modalBody').innerHTML=h;document.getElementById('modal').classList.add('open')}
function closeModal(){document.getElementById('modal').classList.remove('open')}
document.getElementById('modal')?.addEventListener('click',e=>{if(e.target.id==='modal')closeModal()});
function toast(m){alert(m)}
function genericForm(title){return `<div class="notice">${title}</div><div class="form-grid"><div class="field"><label>สถานะ</label><select><option>บันทึกร่าง</option><option>พร้อมดำเนินการ</option></select></div><div class="field"><label>วันที่ดำเนินการ</label><input type="date" value="2026-08-05"></div><div class="field full"><label>รายละเอียด / ความเห็น</label><textarea rows="4"></textarea></div><div class="field full"><label>เอกสารแนบ</label><input type="file" multiple></div></div><div class="actions"><button class="btn btn-secondary" onclick="closeModal()">ยกเลิก</button><button class="btn btn-primary" onclick="toast('บันทึกเรียบร้อย');closeModal()">บันทึก</button></div>`}
function approvalForm(title){return `<div class="notice">${title}</div><div class="field"><label>Checklist การพิจารณา</label><textarea rows="5">✓ ข้อมูลครบถ้วน\n✓ เอกสารถูกต้อง\n✓ ผ่านเงื่อนไขที่กำหนด</textarea></div><div class="field"><label>ความเห็น</label><textarea rows="3"></textarea></div><div class="actions"><button class="btn btn-warn" onclick="toast('ส่งกลับแก้ไขแล้ว');closeModal()">ส่งกลับแก้ไข</button><button class="btn btn-success" onclick="toast('อนุมัติแล้ว');closeModal()">อนุมัติ</button></div>`}
function uploadForm(){return `<div class="field"><label>เลือกเอกสาร PDF/JPG</label><input type="file" multiple accept=".pdf,.jpg,.jpeg,.png"></div><div class="actions"><span></span><button class="btn btn-primary" onclick="toast('อัปโหลดสำเร็จ');closeModal()">อัปโหลด</button></div>`}
